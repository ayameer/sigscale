apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sigscale-wncp.fullname" . }}-test-connection"
  labels:
    {{- include "sigscale-wncp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sigscale-wncp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
