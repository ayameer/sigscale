apiVersion: v1
kind: Service
metadata:
  name: {{ include "sigscale-wncp.fullname" . }}
  labels:
    {{- include "sigscale-wncp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sigscale-wncp.selectorLabels" . | nindent 4 }}
